{
  "name": "vyv-objects-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Pruebas E2E y de experiencia de usuario (Cypress) de la funcionalidad 'objects' (API REST de objetos de XWiki). Requiere el XWiki de docker-compose corriendo en http://localhost:8080 con el flavor instalado.",
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "e2e:api": "cypress run --spec cypress/e2e/objects-api.cy.js",
    "e2e:ux": "cypress run --spec cypress/e2e/objects-ux.cy.js"
  },
  "devDependencies": {
    "cypress": "^13.15.0"
  }
}
